package com.tamj0rd2.ktcheck.current

import com.tamj0rd2.ktcheck.contracts.FlatMapGeneratorContract
import org.junit.jupiter.api.Disabled

internal class FlatMapGeneratorTest : BaseContractImpl(), FlatMapGeneratorContract {
    @Disabled("TODO: If I decide to keep this implementation, I need to fix this test")
    override fun `edge cases combine the outer generators edge cases with the inner generator's derived edge cases`() {
        super.`edge cases combine the outer generators edge cases with the inner generator's derived edge cases`()
    }
}
